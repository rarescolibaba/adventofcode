/** Part One **/

#include <iostream>
#include <fstream>
#include <bitset>
using namespace std;

ifstream fin("input");

int main() {
	bitset<12> x, g, e;
	int f[12] = {0}, gamma = 0, epsilon = 0, step = 1;
	while (fin>>x) {
		for (int i = 11; i >= 0; i--) if (x.test(i)) f[i]++; else f[i]--;
		
	}
	e.set();
	for (int i = 0; i < 12; i++) {
		if (f[i] > 0) g.set(i), e.reset(i);
		gamma += g.test(i) * step;
		epsilon += e.test(i) * step;
		step *= 2;
	}
	cout<<gamma*epsilon<<endl;
}
